# Finance Tracker

Personal finance management app.

## Frontend
SvelteKit — run with `npm run dev` from the `frontend/` folder.

## Backend
Node.js/Express — run with `npm run dev` from the `backend/` folder.